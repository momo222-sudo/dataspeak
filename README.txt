DataSpeak - Turn SQL, R, Python and Excel data into plain English reports.

Setup: add ANTHROPIC_API_KEY to environment, run: pip install -r requirements.txt && python app.py

Deploy free on render.com - connect this GitHub repo, set ANTHROPIC_API_KEY env var, deploy.

Admin stats: /api/admin/stats